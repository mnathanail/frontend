import { ProfileAbstractEdit } from './profile-abstract-edit';

describe('ProfileAbstractEdit', () => {
  it('should create an instance', () => {
    expect(ProfileAbstractEdit).toBeTruthy();
  });
});
